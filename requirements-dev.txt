# Dev/CI зависимости (тесты, линтер, форматтер).
#
# Установка на чистом venv:
#   python -m venv .venv
#   .\.venv\Scripts\activate           # Windows
#   source .venv/bin/activate          # Linux/macOS
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
#
# Не подключайте этот файл в production-окружении.

-r requirements.txt

# ── Тесты ────────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0
# pytest-env: безусловно override DJANGO_SETTINGS_MODULE при запуске тестов.
# Без него env var из шелла (после `manage.py check`) перебивает settings и
# тесты падают на FieldDoesNotExist (PostgreSQL legacy schema vs SQLite test).
# См. H7 в docs/ROADMAP_2026-05_high_medium.md и [tool.pytest_env] в pyproject.toml.
pytest-env>=1.1
# Заморозка времени для billing/storage тестов (используется как @freeze_time).
freezegun>=1.5

# ── Линтер / форматтер ───────────────────────────────────────────────────────
ruff>=0.9.10
