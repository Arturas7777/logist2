# Dev/CI зависимости (тесты, линтер, форматтер).
#
# Установка:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Не подключайте этот файл в production-окружении.

-r requirements.txt

pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0
ruff>=0.9.10
