# Python
*.pyc
__pycache__/
*.py[cod]
*$py.class

# Virtualenv
.venv/
venv/
env/

# Django
*.sqlite3
*.log
*.pot
db.sqlite3
db.sqlite3-journal
local_settings.py

# Static files (collected)
staticfiles/
static_root/

# Media files (опционально, зависит от стратегии)
media/

# Env
.env
.env.local
.env.production
.env.bak*

# Credentials (passwords file)
CREDENTIALS.md
docs/CREDENTIALS.md

# Gmail OAuth secrets — НИКОГДА не коммитить
client_secret*.json
token.json
secrets/

# Revolut Business API — приватный ключ + публичный сертификат для JWT.
# Лежат только на сервере в /var/www/.../logist2/certs/. См. docs/CREDENTIALS.md.
certs/
*.pem
*.cer

# Backup files
*.zip
*.sql
*.sql.gz
*.tar.gz
*.dump
backups/

# System files
.DS_Store
Thumbs.db

# PyCharm / VSCode / IDE files
.idea/
.vscode/
*.swp
*.swo
*~

# Compiled translations
*.mo

# Coverage
.coverage
htmlcov/

# Pytest
.pytest_cache/

# Django file-based cache
.cache/

# Temp commit message
.git_commit_msg.txt

# Test database
test_db.sqlite3

# Node (если используется для frontend)
node_modules/
npm-debug.log
yarn-error.log
container_photos/
.claude/

# Temporary/debug scripts
_*.py
_*.sh
scripts/debug/_*
!scripts/debug/README.md
# Не игнорировать служебные Python-модули внутри пакетов core/.
# (__init__.py и _vehicle_types.py попадают под маску _*.py).
!core/models/_*.py
!core/models/__init__.py
!core/admin/billing/__init__.py
!core/views_website/__init__.py
!core/signals/__init__.py

# Local data (AI RAG index, generated dumps, etc.)
/data/
!/data/.gitkeep

# Ad-hoc script outputs
renumber_out.txt
revolut_expenses/
_backup_*.dump

# Ad-hoc config/files
denis.conf
denis_qr.png

# Ad-hoc analysis outputs (отчёты, генерируемые в локальной разработке).
# Сами скрипты (analysis/*.py) — версионируем; их выходы (json/txt/pdf) — нет.
analysis/*.json
analysis/*.txt
analysis/*.pdf
analysis/IV_*.pdf

# Архивы бэкапов БД (любые форматы)
*_backup*.dump
*_backup*.sql

# IDE / editor temp
*.bak
*.orig

# Temp PowerShell scripts
ps-script-*.ps1