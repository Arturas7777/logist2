"""Ядро AI-агента (планировщик дел на Anthropic).

Модули:

* :mod:`.llm_client` — единый клиент Anthropic: retry, tool use,
  учёт токенов/стоимости в AgentRun, дневной бюджет.
* :mod:`.context_builder` — сборка бизнес-контекста и памяти для промпта.
* :mod:`.memory` — вечная память агента (дистилляция ответов, retrieval).
* :mod:`.email_analyzer` — анализ входящих писем → предложения дел/вопросы.
* :mod:`.task_planner` — утренний дайджест и приоритизация дел.
* :mod:`.agent_tools` — реестр инструментов для tool-use циклов.
* :mod:`.agent_executor` — исполнение дел и одобренных действий.

См. docs/AI_AGENT_PLAN.md — план и текущий статус реализации.
"""
