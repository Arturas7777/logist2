asgiref==3.8.1
attrs==25.3.0
autobahn==24.4.2
Automat==25.4.16
certifi==2025.8.3
cffi==2.0.0
celery[redis]==5.4.0
channels==4.2.0
channels_redis==4.2.1
charset-normalizer==3.4.3
constantly==23.10.4
cryptography==46.0.7
daphne==4.1.2
Django==5.2.14
djangorestframework==3.15.2
gunicorn==23.0.0
hyperlink==21.0.0
idna==3.15
incremental==24.7.2
msgpack==1.1.0
packaging==25.0
pillow>=12.1.0
psycopg2-binary>=2.9.11
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==2.22
pyOpenSSL==26.0.0
python-dotenv==1.2.2
python-slugify==8.0.4
redis>=4.6,<6.0.0
requests==2.33.0
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0
service-identity==24.2.0
setuptools==80.8.0
sqlparse==0.5.4
text-unidecode==1.3
Twisted==26.4.0
txaio==23.1.1
typing_extensions==4.13.2
tzdata==2025.2
urllib3==2.7.0
whitenoise==6.9.0
zope.interface==7.2
# ── Invoice Audit ────────────────────────────────────────────────────────────
pdfplumber>=0.11.0
pymupdf>=1.24.0
anthropic>=0.40.0

# ── AI Chat / RAG (клиентский портал) ────────────────────────────────────────
# Ранее ставился руками на проде; теперь явно зафиксирован в requirements.
openai>=1.40.0

# ── Gmail sync (OAuth) ───────────────────────────────────────────────────────
# Санитайзер HTML для писем, которые рендерятся в карточке контейнера.
bleach>=6.1.0
python-dateutil>=2.8.2
# Автоматическая починка mojibake в письмах (UTF-8→cp1252 и т.п.).
ftfy>=6.3.0

# ── Monitoring ───────────────────────────────────────────────────────────────
sentry-sdk[django,celery]>=2.58.0,<3.0.0
# psutil — для страницы /admin/system-monitor/ (CPU/RAM/disk/процессы/сервисы).
# Используется в core/services/system_monitor.py.
psutil>=5.9.0

# ── Logging ──────────────────────────────────────────────────────────────────
# JSON-форматтер для structured logs (RotatingFileHandler + Loki/CloudWatch).
# Используется в logist2/settings/base.py → LOGGING.formatters.json
# (включается env-флагом LOG_FORMAT=json). См. docs/LOGGING.md.
python-json-logger>=3.1,<4.0
